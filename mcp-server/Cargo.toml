[package]
name = "mcp-server"
version = "0.1.0"
edition = "2021"

[dependencies]
engine = { path = "../engine" }
infrastructure = { path = "../infrastructure" }
tokio = { version = "1.50.0", features = ["macros", "rt-multi-thread"] }
anyhow = "1.0"
